/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["s3.amazonaws.com"], // Specify allowed image domains here
  },
  // Add any other Next.js configuration options you need here
};

export default nextConfig;
